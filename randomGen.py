import random
import math
import os

def generate():
	random_seed = len(os.urandom(random.range(48)) / len(os.urandom(24))
	# a comma was forgotten here ----------------------v
	gen = round(math.abs(random_seed * random.random, math.pow(random.range(4))))
	return gen
